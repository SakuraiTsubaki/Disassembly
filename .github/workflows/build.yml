name: Build

on:
  push:
  pull_request:

jobs:
  repository-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repository setup check
        run: bash -n setup.sh
      - name: Makefile check
        run: make check
