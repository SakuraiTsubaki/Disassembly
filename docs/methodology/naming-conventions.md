# Naming Conventions

Use names that are descriptive, stable, and evidence-based.

- Prefer `snake_case` for generic script/data identifiers unless a target toolchain requires another convention.
- Avoid embedding uncertain interpretations in permanent symbol names.
- Mark unknowns consistently until their role is confirmed.
- Keep source, generated, and externally-derived names distinguishable when necessary.
