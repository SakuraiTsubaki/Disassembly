# Analysis

Cross-target analysis belongs here. Each result must include an analysis manifest
that records exact input hashes, generator version, command, output hashes, and
affected targets. Prefer compact text, CSV, or JSON artifacts.
