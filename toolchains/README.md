# Toolchains

This directory contains reproducible installation/configuration material for disassembly toolchains.

Initial toolchain slots:

- `rgbds/`
- `binutils/`
- `llvm/`
- `ghidra/`
- `radare2/`

Pin versions when a target depends on version-specific behavior. Do not vendor third-party binaries without verifying their redistribution terms.
